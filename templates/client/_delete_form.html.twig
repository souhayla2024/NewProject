<form method="post" action="{{ path('app_client_delete', {'numclt': client.numclt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.numclt) }}">
    <button class="btn">Delete</button>
</form>
